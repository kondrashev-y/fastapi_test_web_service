# Web service whit FastAPI

## To start project

Build the images and run the containers:

```sh
$ docker-compose up -d --build
```

The documentations:

[http://localhost:8002/docs](http://localhost:8002/docs)